name: logger_kit
description: A comprehensive Flutter logging toolkit with namespace, interceptors, privacy filter and more. Supports console, file, and remote logging.
version: 1.1.0
homepage: https://github.com/h1s97x/LoggerKit

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
